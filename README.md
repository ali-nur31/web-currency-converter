# Figma макет

https://www.figma.com/design/iNwU0WqkemjpKaxECFLPEV/Currency-Converter--Community-?node-id=0-1&t=sZySKseAIjmny02m-1

---

# 💱 Currency Converter

Простое веб-приложение для конвертации валют в реальном времени. Использует ExchangeRate-API, поддерживает все мировые валюты и отображает флаги выбранных стран. Интерфейс адаптирован под разные устройства и выполнен в чистом минималистичном стиле.

---

## 🚀 Возможности

* Конвертация валют в реальном времени
* Использование всех валют, доступных в ExchangeRate-API
* Отображение флагов выбранных валют
* Быстрая кнопка для обмена валют местами
* Автоматическая подстановка курса
* Простая и лёгкая адаптивная верстка

---

## 🛠 Технологии

* **HTML** — структура интерфейса
* **CSS** — стилизация приложения
* **JavaScript** — логика конвертации и обработки событий
* **ExchangeRate-API** — источник реальных курсов валют

---

## 📦 Установка и запуск

1. Скачайте или клонируйте проект

   ```bash
   git clone https://github.com/your-name/currency-converter.git
   ```

2. Перейдите в папку проекта

   ```bash
   cd currency-converter
   ```

3. Добавьте свой API ключ в `script.js`

   ```js
   const API_KEY = "your_api_key_here";
   ```

4. Откройте `index.html` в браузере

   * двойным кликом
   * или через локальный сервер:

   ```bash
   npx serve .
   ```

   или

   ```bash
   python -m http.server 5500
   ```

---

## 📂 Структура проекта

```
currency-converter/
│── index.html
│── styles.css
│── script.js
└── README.md
```

---

## 🧠 Как это работает

Приложение загружает список всех валют через:

```js
fetch(`https://v6.exchangerate-api.com/v6/${API_KEY}/codes`)
```

События и логика приложения:

* Авто-загрузка валют и вставка в `<select>`
* Выбор валют и отображение флагов
* Сделка запроса курса валют через

  ```js
  fetch(`https://v6.exchangerate-api.com/v6/${API_KEY}/latest/${from}`)
  ```
* Подсчет результата с точностью до двух знаков
* Кнопка обмена валют местами
* Валидация введенного числа

---

## 📜 Лицензия

Свободно используйте, улучшайте и адаптируйте под свои проекты.
